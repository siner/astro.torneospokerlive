---
import { getPastEventos } from '../../lib/api'
import RowEvento from './RowEvento'

const eventos = await getPastEventos()
---

{eventos.map((event) => <RowEvento evento={event} showtour={true} />)}
