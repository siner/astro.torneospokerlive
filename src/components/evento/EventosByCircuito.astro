---
const { circuito } = Astro.props
import { getEventosByCircuito } from '../../lib/api'
import RowEvento from './RowEvento'
const events = await getEventosByCircuito(circuito.id)
---

{
    events.map((event) => (
        <RowEvento key={event.id} evento={event} showtour={false} />
    ))
}
