---
import { getTomorrowTorneos } from '../../lib/api'
import RowTournament from './RowTournament'

const { datamapped: torneos, meta } = await getTomorrowTorneos()
---

{
    torneos?.map((torneo) => (
        <RowTournament
            key={torneo.id}
            torneo={torneo}
            casino="true"
            event="true"
        />
    ))
}
